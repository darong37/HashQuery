# delete サポート実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `delete` DSL を追加し、`as` 変数の戻り値をハッシュリファレンスに変更する

**Architecture:** `src/HashQuery.pm` の `query` に delete モードを追加。`as` 変数の完了後代入を `{ count => N, affect => M }` に変更。`delete` は引数なしの DSL 関数として `select` と同列に扱う。

**Tech Stack:** Perl, Test::More, Clone

---

### Task 1: `as` 変数をハッシュリファレンスへ変更

**Files:**
- Modify: `src/HashQuery.pm:81`
- Modify: `test/hashquery.t:852-856`

- [ ] **Step 1: 既存の `as` テストを新しい期待値に更新する**

`test/hashquery.t` の852〜856行目を以下に書き換える:

```perl
subtest 'as: クエリ完了後にレコード数が格納される' => sub {
    our $tc;
    query \@base, as $tc, where { $_->{b} == 10 };
    is $tc->{count},  2;
    is $tc->{affect}, 2;
};
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
perl test/hashquery.t 2>&1 | grep -A3 'クエリ完了後'
```

期待: FAIL（`$tc->{count}` が undef）

- [ ] **Step 3: `query` の `as` 代入をハッシュリファレンスへ変更する**

`src/HashQuery.pm` の81行目を書き換える:

```perl
    if ($as) {
        ${ $as->{alias} } = {
            count  => scalar @$tbl,
            affect => scalar @$tbl,
        };
    }
    return $tbl;
```

（旧: `${ $as->{alias} } = scalar @$tbl if $as;`）

- [ ] **Step 4: テストが全て通ることを確認する**

```bash
perl test/hashquery.t 2>&1 | tail -5
```

期待: `ok 93 - as: クエリ完了後にレコード数が格納される` を含む `1..93`

- [ ] **Step 5: コミット**

```bash
git add src/HashQuery.pm test/hashquery.t
git commit -m "as: change post-query variable to hashref {count, affect}"
```

---

### Task 2: `delete` DSL 関数を追加する

**Files:**
- Modify: `src/HashQuery.pm:9-21`（`@EXPORT`）
- Modify: `src/HashQuery.pm`（`sub delete` 追加）
- Modify: `test/hashquery.t`（テスト追加）

- [ ] **Step 1: `delete` DSL の単体テストを書く**

`test/hashquery.t` の `done_testing;` の直前に追加する:

```perl
# ===========================================================================
# delete — DSL ノード
# ===========================================================================

subtest 'delete: DSL ノードを返す' => sub {
    my $d = delete();
    is_deeply $d, { delete => 1 };
};
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
perl test/hashquery.t 2>&1 | grep -A3 'DSL ノードを返す'
```

期待: FAIL

- [ ] **Step 3: `@EXPORT` に `delete` を追加し `sub delete` を実装する**

`src/HashQuery.pm` の `@EXPORT` を更新:

```perl
our @EXPORT = qw(
    query
    as
    select
    delete
    where
    having
    count_by
    max_by
    min_by
    first_by
    last_by
    grep_concat
);
```

`sub having` の直後に追加:

```perl
sub delete () {
    return { delete => 1 };
}
```

- [ ] **Step 4: テストが全て通ることを確認する**

```bash
perl test/hashquery.t 2>&1 | tail -5
```

期待: `1..94`（1件増加）

- [ ] **Step 5: コミット**

```bash
git add src/HashQuery.pm test/hashquery.t
git commit -m "delete: add delete DSL function"
```

---

### Task 3: `query` が `delete` ノードを解釈し `select` との競合を検出する

**Files:**
- Modify: `src/HashQuery.pm:23-83`（`query` 関数）
- Modify: `test/hashquery.t`（テスト追加）

- [ ] **Step 1: `select` と `delete` の同時指定で die するテストを書く**

`test/hashquery.t` の `done_testing;` の直前に追加:

```perl
subtest 'delete: select と同時に指定するとdieする' => sub {
    eval { query \@base, select, delete };
    like $@, qr/select and delete cannot be used together/;
};

subtest 'delete: except と同時に指定するとdieする' => sub {
    eval { query \@base, select { except => ['c'] }, delete };
    like $@, qr/select and delete cannot be used together/;
};
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
perl test/hashquery.t 2>&1 | grep -A3 'select と同時'
```

期待: FAIL

- [ ] **Step 3: `query` の DSL 解釈に `delete` 対応を追加する**

`src/HashQuery.pm` の `query` 関数の変数宣言と DSL ループを以下に書き換える:

```perl
sub query ($@) {
    my ($table, @dsls) = @_;

    die 'query requires an Array of Hash table'
        unless ref $table eq 'ARRAY';

    my @all = _check_cols($table);
    my ($as, $sel, $exc, $whr, $hvg, $del);

    for my $dsl (@dsls) {
        die 'invalid DSL part'
            unless ref $dsl eq 'HASH';

        if (exists $dsl->{alias}) {
            $as = $dsl;
        }
        elsif (exists $dsl->{select}) {
            $sel = $dsl;
        }
        elsif (exists $dsl->{except}) {
            $exc = $dsl;
        }
        elsif (exists $dsl->{where}) {
            $whr = $dsl;
        }
        elsif (exists $dsl->{having}) {
            $hvg = $dsl;
        }
        elsif (exists $dsl->{delete}) {
            $del = $dsl;
        }
        else {
            die 'invalid DSL part';
        }
    }

    die 'select and delete cannot be used together'
        if ($sel || $exc) && $del;
```

- [ ] **Step 4: テストが全て通ることを確認する**

```bash
perl test/hashquery.t 2>&1 | tail -5
```

期待: `1..96`

- [ ] **Step 5: コミット**

```bash
git add src/HashQuery.pm test/hashquery.t
git commit -m "delete: parse delete DSL node, die on select+delete conflict"
```

---

### Task 4: `delete` 実行モードを実装する

**Files:**
- Modify: `src/HashQuery.pm`（`query` 関数の実行部）
- Modify: `test/hashquery.t`（テスト追加）

- [ ] **Step 1: `delete` の動作テストを書く**

`test/hashquery.t` の `done_testing;` の直前に追加:

```perl
# ===========================================================================
# delete — 実行
# ===========================================================================

subtest 'delete: where にマッチした行を削除して残りを返す' => sub {
    my $r = query \@base, delete, where { $_->{b} == 10 };
    is scalar @$r, 3;
    my @a_vals = map { $_->{a} } @$r;
    is_deeply [sort @a_vals], [qw/bob carol eve/];
};

subtest 'delete: 条件なしで全行削除する' => sub {
    my $r = query \@base, delete;
    is scalar @$r, 0;
};

subtest 'delete: 一致なしで全行残る' => sub {
    my $r = query \@base, delete, where { $_->{b} > 999 };
    is scalar @$r, 5;
};

subtest 'delete: having と組み合わせて削除できる' => sub {
    my $r = query \@base, delete, having { count_by('b') > 1 };
    is scalar @$r, 1;
    is $r->[0]{a}, 'carol';
};

subtest 'delete: 元テーブルは変更されない' => sub {
    my @orig = ({ a => 1, b => 2 }, { a => 3, b => 4 });
    my @copy = map { +{ %$_ } } @orig;
    query \@orig, delete, where { $_->{a} == 1 };
    is_deeply \@orig, \@copy;
};

subtest 'delete: _idx は出力に含まれない' => sub {
    my $r = query \@base, delete, where { $_->{b} > 999 };
    ok !exists $r->[0]{_idx};
};

subtest 'delete: as で count と affect が返る' => sub {
    our $td;
    query \@base, as $td, delete, where { $_->{b} == 10 };
    is $td->{count},  3;
    is $td->{affect}, 2;
};

subtest 'delete: select と同じ条件で対称動作する' => sub {
    my $selected = query \@base, select, where { $_->{b} == 10 };
    my $deleted  = query \@base, delete, where { $_->{b} == 10 };
    is scalar @$selected + scalar @$deleted, 5;
};
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
perl test/hashquery.t 2>&1 | grep -A3 'マッチした行を削除'
```

期待: FAIL

- [ ] **Step 3: `query` に delete 実行モードを実装する**

`src/HashQuery.pm` の `query` 関数の実行部（`$tbl = clone(...)` 以降）を以下に書き換える:

```perl
    my $tbl = clone($table);

    for my $i (0 .. $#$tbl) {
        $tbl->[$i]{_idx} = $i;
    }

    if ($del) {
        my $matched = $tbl;
        $matched = _run_where($matched, $as, $whr) if $whr;
        $matched = _run_having($matched, $as, $hvg) if $hvg;

        my %del_idx = map { $_->{_idx} => 1 } @$matched;
        my @remaining = grep { !$del_idx{ $_->{_idx} } } @$tbl;
        my $result = _run_select(\@remaining, \@all);

        if ($as) {
            ${ $as->{alias} } = {
                count  => scalar @$result,
                affect => scalar @$matched,
            };
        }
        return $result;
    }

    my $cols;

    if ($exc) {
        my %skip = map { $_ => 1 } @{ $exc->{except} };
        $cols = [ grep { !$skip{$_} } @all ];
    }
    elsif (!$sel || $sel->{select} eq '*') {
        $cols = \@all;
    }
    elsif (ref $sel->{select} eq 'ARRAY') {
        $cols = $sel->{select};
    }
    else {
        die 'invalid select node';
    }

    $tbl = _run_where($tbl, $as, $whr) if $whr;
    $tbl = _run_having($tbl, $as, $hvg) if $hvg;
    $tbl = _run_select($tbl, $cols);

    if ($as) {
        ${ $as->{alias} } = {
            count  => scalar @$tbl,
            affect => scalar @$tbl,
        };
    }
    return $tbl;
}
```

- [ ] **Step 4: テストが全て通ることを確認する**

```bash
perl test/hashquery.t 2>&1 | tail -5
```

期待: `1..104`（8件増加）

- [ ] **Step 5: コミット**

```bash
git add src/HashQuery.pm test/hashquery.t
git commit -m "delete: implement delete execution mode"
```

---

### Task 5: ドキュメントを更新してプッシュする

**Files:**
- Modify: `docs/spec.md`
- Modify: `docs/test-spec.md`
- Modify: `README.md`
- Modify: `README.ja.md`

- [ ] **Step 1: `docs/spec.md` を更新する**

以下の変更を行う:

**セクション 1（提供関数一覧）** に `delete` を追加:

```markdown
| `delete` | DSL | 削除条件を宣言する（`where` / `having` と組み合わせて使う） |
```

**セクション 2（DSL 構文）** のプロトタイプ定義に追加:

```perl
sub delete ();
```

**セクション 3（各関数リファレンス）** に `### delete` を追加（`select` の直後）:

```markdown
### delete

**シグネチャ:**

```perl
delete;
```

**戻り値:**

```perl
{ delete => 1 }
```

**動作説明:**

`query` に渡すことで削除モードを宣言する。`where` / `having` にマッチした行を削除対象とし、残存行を AOH で返す。元のテーブルは変更しない。`select` / `except` と同時に指定した場合は die する。`select` と対称的な操作であり、同じ条件で `select` ↔ `delete` を差し替えることで削除対象の確認と実行を切り替えられる。

**コード例:**

```perl
# select で確認
my $preview = query $table, select, where { $_->{b} > 10 };

# delete に差し替えて実行
my $result = query $table, delete, where { $_->{b} > 10 };
```
```

**セクション 3（as）** のクエリ完了後の動作説明を更新:

旧:
```
クエリ完了後、指定した変数には出力テーブルのレコード数（スカラ値）が格納される。
```

新:
```
クエリ完了後、指定した変数にはハッシュリファレンス `{ count => N, affect => M }` が格納される。`count` は結果 AOH の行数、`affect` は実際に変化した行数（select モードでは count と同値、delete モードでは削除行数）。
```

**セクション 3（as）** のコード例を更新:

```perl
our $tbl;
my $result = query $table, as $tbl, where { $tbl->{score} >= 80 };
# クエリ完了後 $tbl->{count} には結果行数、$tbl->{affect} には変化行数が格納される
```

**セクション 7（実行モデル）** に delete モードの処理順序を追加:

```markdown
### delete モード

1. `as` 解釈（エイリアス変数の確定）
2. 各行への `_row` 付加（0 始まりの行番号）
3. `where` 評価（削除候補を特定）
4. `having` 前計算 → 評価（削除候補をさらに絞り込み）
5. 削除対象行を除いた残存行を AOH として返す（`_row` を除外）
6. `as` 変数に `{ count => 残存行数, affect => 削除行数 }` を格納
```

**セクション 8（制約・注意事項）** に追加:

```
- `select`（または `except`）と `delete` を同時に指定すると die する
- `delete` モードでは列射影を行わない（全列を返す）
- `delete` モードでも元テーブルは変更されない（非破壊的操作）
```

- [ ] **Step 2: `docs/test-spec.md` を更新する**

`### 7. 組み合わせ` の前に新しいセクションを追加:

````markdown
### 8. delete

| No. | 説明 | 入力 / 条件 | 期待結果 |
|-----|------|------------|---------|
| 94 | where にマッチした行を削除して残りを返す | `@base`、`delete`、`where { $_->{b} == 10 }` | 行数 3、a: bob, carol, eve |
| 95 | 条件なしで全行削除する | `@base`、`delete` | 行数 0 |
| 96 | 一致なしで全行残る | `@base`、`delete`、`where { $_->{b} > 999 }` | 行数 5 |
| 97 | having と組み合わせて削除できる | `@base`、`delete`、`having { count_by('b') > 1 }` | 行数 1、a = 'carol' |
| 98 | 元テーブルは変更されない | `@base` で delete 実行後 | `@base` の内容が変化しない |
| 99 | `_idx` は出力に含まれない | `@base`、`delete`、`where { $_->{b} > 999 }` | 先頭行に `_idx` キーが存在しない |
| 100 | as で count と affect が返る | `@base`、`as $td`、`delete`、`where { $_->{b} == 10 }` | `$td->{count}` = 3、`$td->{affect}` = 2 |
| 101 | select と同じ条件で対称動作する | `@base` に対して select と delete を同じ where 条件で実行 | 両者の行数の合計が 5 |
````

また既存の `as` セクション（No.23）の期待結果を更新:

旧:
```
| 23 | クエリ完了後にレコード数が格納される | ... | クエリ後 `$tc` に `2` が格納される |
```

新:
```
| 23 | クエリ完了後にレコード数が格納される | ... | クエリ後 `$tc->{count}` = 2、`$tc->{affect}` = 2 |
```

- [ ] **Step 3: `README.md` の Features テーブルに `delete` を追加する**

```markdown
| `delete` | Declares delete mode; rows matching `where` / `having` are removed, remaining rows returned |
```

`select` の行の直後に追加する。

- [ ] **Step 4: `README.ja.md` の主な機能テーブルに `delete` を追加する**

```markdown
| `delete` | 削除モードを宣言する。`where` / `having` にマッチした行を削除し、残存行を返す |
```

`select` の行の直後に追加する。

- [ ] **Step 5: テストが全て通ることを確認する**

```bash
perl test/hashquery.t 2>&1 | tail -3
```

期待: `1..104`

- [ ] **Step 6: コミット**

```bash
git add docs/spec.md docs/test-spec.md README.md README.ja.md
git commit -m "docs: update spec, test-spec, and README for delete support"
```

- [ ] **Step 7: プッシュ**

```bash
git push origin main
```
